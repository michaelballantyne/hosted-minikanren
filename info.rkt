#lang info

(define name "minikanren-ee")

(define deps '("base"
               "rackunit-lib"
               ["ee-lib" #:version "1.0"]))

;(define build-deps '("racket-doc" "scribble-lib"))
;(define scribblings '(("main.scrbl" ())))
